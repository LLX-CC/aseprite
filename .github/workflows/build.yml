name: Build Aseprite (Release)

on:
  workflow_dispatch: # 支持手动触发

jobs:
  build:
    runs-on: windows-latest
    steps:
    - name: Checkout Aseprite
      uses: actions/checkout@v4
      with:
        submodules: 'recursive'

    - name: Install Skia
      shell: bash
      run: |
        this_dir=$(cygpath "${{ github.workspace }}")
        skia_url=$(source $this_dir/laf/misc/skia-url.sh | xargs)
        skia_file=$(basename $skia_url)
        echo "正在下载 Skia: $skia_url"
        curl --ssl-revoke-best-effort -L -o "$skia_file" "$skia_url"
        unzip "$skia_file" -d skia

    - name: Set up Ninja
      uses: aseprite/get-ninja@main

    - name: Set up MSVC
      uses: TheMrMilchmann/setup-msvc-dev@v4
      with:
        arch: x64

    - name: Generating Makefiles
      shell: bash
      run: |
        cmake -S . -B build -G Ninja \
          -DCMAKE_BUILD_TYPE=RelWithDebInfo \
          -DENABLE_TESTS=OFF \
          -DENABLE_SCRIPTING=ON \
          -DLAF_BACKEND=skia \
          -DSKIA_DIR=$(realpath skia) \
          -DSKIA_LIBRARY_DIR=$(realpath skia/out/Release-x64)

    - name: Compiling
      shell: bash
      run: |
        cd build && ninja aseprite

    - name: Create Release Package
      run: |
        mkdir dist
        copy build\bin\aseprite.exe dist\
        xcopy /E /I build\bin\data dist\data
        powershell Compress-Archive -Path dist\* -DestinationPath aseprite-windows.zip

    - name: Upload Artifact
      uses: actions/artifact@v4
      with:
        name: aseprite-windows
        path: aseprite-windows.zip
